version: 2.1
orbs:
  terraform: ovotech/terraform@dev:better-errors

jobs:
  tf_validate_plan:
    executor: terraform/default
    steps:
      - run:
          name: stuff
          command: |
            touch test.tf
      - terraform/plan:
          path: .

workflows:
   full:
     jobs:
       - tf_validate_plan
