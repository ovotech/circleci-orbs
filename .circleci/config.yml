version: 2.1
orbs:
  terraform: ovotech/terraform@dev:tfmask

jobs:
  tf_validate_plan:
    executor: terraform/default
    steps:
      - checkout
      - terraform/plan:
          path: .

workflows:
   full:
     jobs:
       - tf_validate_plan
