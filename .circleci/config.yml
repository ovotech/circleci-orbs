version: 2.1
orbs:
  terraform: ovotech/terraform@1

jobs:
  tf_validate_plan:
    docker:
      - image: eversc/terraform
    steps:
      - run:
          name: stuff
          command: |
            touch test.tf
      - terraform/plan:
          path: .

workflows:
  version: 2
   full:
     jobs:
       - tf_validate_plan
